<h1>Editing selection</h1>

<%= render 'form' %>

<%= link_to 'Back', selections_path %> |
<%= link_to 'Delete', @selection, method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Create new Step', new_steps_path(@selection) %>